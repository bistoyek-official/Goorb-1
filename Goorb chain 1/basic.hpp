#include<iostream>
#include<vector>
#include<string>
#include<fstream>
#include<time.h>
#include<conio.h>
#include<algorithm>
using namespace std;

/*
	tb = (time_begin_mining) % (2^32) -> hamoon moghe bezanan time(0)
	tbr = when it sended
		R(tbr) = {(r e R) => (r = (tb, maxn, N, M, same, addr, addr1, bl, blsc) & some_property(r, tbr))}
		
*/